# Deprecated development example kept only as a compatibility reference.
# Canonical runtime definition: inventory-api/Dockerfile
FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache python3 make g++

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 2500

CMD ["node", "src/server.js"]